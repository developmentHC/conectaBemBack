# Claude Guide - conecta-bem/api

Read `AGENTS.md` first. It is the canonical guide for this backend repository and
contains the stack, source map, commands, architecture rules, generated-file rules,
and validation expectations.

If a task affects frontend contracts, generated clients, auth, CORS, webhooks, or
ports, also read `../AGENTS.md` and coordinate with `../web/AGENTS.md`.
